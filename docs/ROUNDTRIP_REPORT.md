# Round-trip conformance report

<!-- Generated by scripts/generate-roundtrip-report.js via `npm run test:roundtrip`. Do not edit by hand. -->

**Property under test**: `unwrapTablesForHost(serialize(parse(wrapTablesForEditor(input)))) === input` — byte equality, including trailing whitespace
and final-newline state. This is the exact pipeline the editor runs between the host
document and the ProseMirror view (`src/webview/editor/markdown-codec.ts`).

**Corpus**: 53 cases — 49 stored fixtures plus 4
synthesized byte-fragile variants (CRLF, trailing space, two-space hard break, missing
final newline) that editors would silently normalize if stored on disk. This is the
hand-curated construct corpus from issue #245, not the full CommonMark 0.31.2 example
suite (that larger corpus is spec requirement R-01 and tracked separately).

## Results

| Verdict | Cases |
| --- | ---: |
| Byte-identical | 1 |
| Final-newline-only deviation (single root cause, #282) | 28 |
| Construct deviations | 24 |
| **Total** | **53** |

**Reading the numbers honestly**: the markdown construct survives byte-identically in
29 of 53 cases; the missing trailing newline (#282) is a single
pure-serializer defect that still blocks every stored fixture from a strict codec pass,
but the host save path now preserves the existing final-newline state for real files. The
24 construct deviations cluster into 8 root causes, each with a tracking issue
(#283–#289). Until #283/#284 are fixed, hard-wrapped paragraphs unfold and dash bullets,
underscore emphasis, and setext headings are rewritten to canonical forms.

## Per-category breakdown

| Category | Cases | Byte-identical | Final-newline-only | Construct deviations |
| --- | ---: | ---: | ---: | ---: |
| blockquotes | 3 | 0 | 1 | 2 |
| breaks | 2 | 0 | 1 | 1 |
| code | 1 | 0 | 0 | 1 |
| code-blocks | 5 | 0 | 3 | 2 |
| emphasis | 4 | 0 | 3 | 1 |
| entities | 1 | 0 | 0 | 1 |
| escapes | 1 | 0 | 0 | 1 |
| front-matter | 1 | 0 | 0 | 1 |
| headings | 3 | 0 | 1 | 2 |
| html | 2 | 0 | 1 | 1 |
| images | 1 | 0 | 1 | 0 |
| line-endings | 1 | 0 | 0 | 1 |
| links | 4 | 0 | 3 | 1 |
| lists | 8 | 0 | 5 | 3 |
| mixed | 1 | 0 | 1 | 0 |
| paragraphs | 2 | 0 | 1 | 1 |
| strikethrough | 1 | 0 | 0 | 1 |
| tables | 5 | 0 | 5 | 0 |
| task-lists | 1 | 0 | 0 | 1 |
| thematic-breaks | 2 | 0 | 1 | 1 |
| unicode | 1 | 0 | 1 | 0 |
| whitespace | 3 | 1 | 0 | 2 |

## Deviation index

| Fixture | Kind | Root cause | Issue |
| --- | --- | --- | --- |
| `blockquotes--lazy.md` | construct | lazy continuation lines are absorbed into the marked paragraph and the soft break joins them | #283 |
| `blockquotes--nested.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `blockquotes--simple.md` | construct | soft line breaks inside the quoted paragraph parse to a plain space; the two marked lines join into one | #283 |
| `breaks--hard-backslash.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `breaks--hard-two-spaces (synthesized)` | construct | hard-break marker style is not recorded; the serializer emits the backslash form | #284 |
| `code--span.md` | construct | code spans containing backticks are padded with spaces by the serializer | #284 |
| `code-blocks--fenced-language.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `code-blocks--fenced-no-language.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `code-blocks--fenced-tilde.md` | construct | fence style is not recorded; the serializer emits backtick fences | #284 |
| `code-blocks--indented.md` | construct | indented code blocks are not distinguished from fenced ones; the serializer emits backtick fences | #284 |
| `code-blocks--mermaid.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `emphasis--adjacent-nested.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `emphasis--asterisk.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `emphasis--intraword-underscores.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `emphasis--underscore.md` | construct | emphasis marker style is not recorded; the serializer emits * forms | #284 |
| `entities--named.md` | construct | HTML entities are decoded at parse and serialized as raw characters | #285 |
| `escapes--punctuation.md` | construct | backslash escapes are decoded at parse and re-escaped per serializer policy (escaped # comes back unescaped) | #285 |
| `front-matter--yaml.md` | construct | no front-matter support; the opening --- parses as a thematic break and the closing --- turns the YAML body into a setext heading | #289 |
| `headings--atx-closing-hashes.md` | construct | ATX closing hash sequences are not recorded; the serializer emits the open form | #284 |
| `headings--atx.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `headings--setext.md` | construct | heading source form is not recorded; the serializer emits ATX headings | #284 |
| `html--block.md` | construct | html:false parses raw HTML as paragraph text; tags survive but the block line structure collapses via soft-break joining | #283 |
| `html--inline.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `images--inline.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `line-endings--crlf (synthesized)` | construct | the editor pipeline is LF-internal (wrapTablesForEditor splits and rejoins with LF; markdown-it normalizes EOLs); CRLF input serializes as LF | #288 |
| `links--autolink.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `links--bare-url.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `links--inline.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `links--reference.md` | construct | reference link definitions are resolved at parse and dropped; links serialize inline | #286 |
| `lists--bullet-dash.md` | construct | list marker style is not recorded; the serializer emits * | #284 |
| `lists--bullet-plus.md` | construct | list marker style is not recorded; the serializer emits * | #284 |
| `lists--bullet-star.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `lists--loose.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `lists--nested.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `lists--ordered-paren.md` | construct | ordered-list delimiter style is not recorded; the serializer emits the dot form | #284 |
| `lists--ordered-start-offset.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `lists--ordered.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `mixed--readme-sample.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `paragraphs--plain.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `paragraphs--soft-wrapped.md` | construct | soft line breaks parse to a plain space, so author line-wrapping unfolds on serialize | #283 |
| `strikethrough--tildes.md` | construct | no strikethrough schema support; tilde characters are backslash-escaped by the serializer | #287 |
| `tables--alignment.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `tables--escaped-pipe.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `tables--no-outer-pipes.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `tables--ragged.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `tables--simple.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `task-lists--basic.md` | construct | no task-list schema support; the literal checkbox brackets are backslash-escaped by the serializer | #287 |
| `thematic-breaks--dashes.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `thematic-breaks--variants.md` | construct | thematic break markup is not recorded; the serializer always emits --- | #284 |
| `unicode--emoji-cjk.md` | final-newline-only | prosemirror-markdown serialize() emits no trailing newline; the construct content itself round-trips byte-identically | #282 |
| `whitespace--blank-lines.md` | construct | blank-line runs are not represented; block separation serializes as exactly one blank line | #284 |
| `whitespace--trailing-single-space (synthesized)` | construct | the parser strips the trailing space and the soft break joins the lines | #283 |

Full input/output samples: `tests/unit/round-trip/KNOWN_DEVIATIONS.md`.
Run locally: `npm run test:roundtrip`.
